// Skeleton component — animated loading placeholder for content areas.
import type { SkeletonProps } from './Skeleton.types';

export function Skeleton(_props: SkeletonProps): null {
  return null;
}
